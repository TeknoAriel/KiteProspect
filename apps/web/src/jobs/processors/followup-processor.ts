import type { FollowupJobPayload } from "../job-payloads";

/** Placeholder Fase 5 — cron/BullMQ de seguimiento real en otra entrega. */
export async function processFollowupJob(_payload: FollowupJobPayload): Promise<void> {
  return;
}
